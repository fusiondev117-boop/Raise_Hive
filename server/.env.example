# Server Configuration
PORT=3001

# MongoDB Atlas Configuration (Free Online Database - VPN Compatible)
# Get your connection string from: https://cloud.mongodb.com
# 1. Create free cluster at MongoDB Atlas
# 2. Create database user
# 3. Whitelist IP: 0.0.0.0/0 (allow from anywhere)
# 4. Get connection string and replace <password> with your password
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/raisehive?retryWrites=true&w=majority

# Email Configuration (Gmail)
EMAIL_USER=hello@raisehive.io
EMAIL_PASS=your-gmail-app-password

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
