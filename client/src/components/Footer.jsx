// This component is deprecated - Footer is now in App.jsx
// Keeping this file to prevent import errors in existing pages
import React from "react";

const Footer = () => {
  // Return null since footer is now global in App.jsx
  return null;
};

export default Footer;
